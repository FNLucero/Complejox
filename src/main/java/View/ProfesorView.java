package View;

import org.uqbar.arena.windows.WindowOwner;

import ViewModel.ProfesorViewModel;

public class ProfesorView extends ProfesorViewModel{
	
	  public ProfesorView(WindowOwner owner) {
		    super(owner, new ProfesorViewModel());
		  }
	
}
